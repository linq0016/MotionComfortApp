# Legacy

Archived experimental modules and tests that are no longer part of the active MotionComfort product targets live here for reference only.

- `Core/` contains old cue-state and layout helpers that are no longer consumed by the production session chain.
- `Visual/` contains retired visual experiments that are not compiled into the current app.
- `Tests/` contains tests that only covered the archived legacy models.
